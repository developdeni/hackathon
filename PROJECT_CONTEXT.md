# Tanap AI — Единый документ проекта

> **Единый источник правды (Single Source of Truth) для команды и AI-ассистентов.**  
> Дедлайн хакатона: **21 сентября 2026, 23:59**. Основной трек: **Трек 1 — GIS и дистанционное зондирование**.

---

## 1. О продукте и цепочке ценности

**Tanap AI** — мобильный терминал агрономического мониторинга для фермеров и агрономов (Акмолинская область, Казахстан).
**Главная продуктовая цепочка:**
> **Спутник (Sentinel-2)** → **Детекция аномалий вегетации (NDVI/NDMI)** → **Выделение устойчивых очагов риска** → **Диспетчеризация агронома** → **Полевой выезд с фото и GPS** → **Локальная история и валидация**.

---

## 2. Стек технологий и архитектура

- **Mobile Client:** React Native (Expo SDK 57 / TypeScript), iOS Native Release build с вшитым Hermes `main.jsbundle`.
  - Целевое устройство: iPhone 13 (`Danil`, `00008110-001425580A50201E`).
  - Шрифты: Google Fonts **Montserrat** (`Montserrat_400Regular`, `Montserrat_500Medium`, `Montserrat_600SemiBold`, `Montserrat_700Bold`).
  - UI стиль: iOS Inset Grouped, строгий промышленный интерфейс без декоративного AI-слопа.
  - Сеть: `EXPO_PUBLIC_API_URL` (по умолчанию `http://192.168.8.100:8000`).
  - Хранение токена: `@react-native-async-storage/async-storage`.
- **Backend:** FastAPI (Python 3.14), Uvicorn (`0.0.0.0:8000`), SQLite (`backend/data/tanap.db`), фото в `backend/data/uploads/`.
  - Авторизация: Argon2 password hashing + JWT (python-jose).
  - Геометрия: точный расчёт площади (сферическая формула Гаусса) и периметра (Haversine).
  - Аналитика: детерминированный временной ряд NDVI/NDMI, детекция зон риска, погодные индексы Open-Meteo.

---

## 3. Текущее состояние экранов и функционала

| Экран / Модуль | Файл | Статус и возможности |
|---|---|---|
| **Авторизация** | `mobile/app/auth/login.tsx`, `register.tsx` | Вход и регистрация, Argon2 + JWT, валидация полей, сброс истории навигации (кнопки назад нет). |
| **Профиль пользователя** | `mobile/app/profile/me.tsx` | Буквенный аватар (72px), сетка статистики (поля, га, осмотры), данные аккаунта (без обрезания текста), выход. |
| **Главный экран** | `mobile/app/index.tsx` | Выбор хозяйства, суммарная статистика, список участков с бейджами культур, быстрый переход в профиль, статус API. |
| **Создание / Редактирование поля** | `mobile/app/field/new.tsx` | Интерактивная карта: перетаскивание 4 углов (`draggable Marker`), ручной ввод координат и метража, GPS-центрирование, живой расчёт га, автоконтур по клику/центру через Sentinel-2 Process API (NDVI-растр, flood-fill и edge bbox) с ручной проверкой перед сохранением. |
| **Карточка поля** | `mobile/app/field/[id].tsx` | 8 блоков: инфо поля, классификация «в обороте / залежь» по амплитуде NDVI, спутниковый статус (NDVI/NDMI/очаги), агрометеорология Open-Meteo, карта со слоями, пиллы зон риска, выгрузки GeoJSON/CSV/PDF и журнал осмотров. |
| **Новый осмотр** | `mobile/app/field/[id]/new-inspection.tsx` | Фото с камеры / галереи, автоподстановка координат и привязка к очагу риска, текстовая заметка, сохранение в SQLite. |
| **Детали осмотра** | `mobile/app/inspection/[id].tsx` | Полноэкранный просмотр фото, геопозиция, статус синхронизации. |

---

## 4. Дизайн-система (Montserrat, iOS Inset Grouped, Anti-Slop)

- **Палитра (60 / 30 / 10):**
  - Фон (`60%`): `#F2F2F7` (System Grouped Background).
  - Карточки (`30%`): `#FFFFFF`, скругления `10–12px`, разделители `#E5E5EA` (`hairlineWidth`).
  - Акцент (`10%`): `#1B5E20` (Dark Forest Green), нажатие `#0E3A14`, мягкий фон `#E8F5E9`.
  - Текст: основной `#1C1C1E`, вторичный `#636366`, подписи `#8E8E93`.
- **Правила Anti-Slop:**
  - ❌ Никаких неоновых градиентов, фиолетовых свечений и размытий.
  - ❌ Никаких бессмысленных эмодзи в роли иконок (`🌱`, `✨`, `🤖`).
  - ❌ Запрещены вложенные карточки в карточках.
  - ❌ Тексты строго утилитарные: «Поле», «Культура», «Площадь», без маркетинговой воды.

---

## 5. Интеграции и источники данных

1. **Copernicus Sentinel-2 L2A (10м)**:
   - Статус: **ПОДКЛЮЧЁН И АКТИВЕН** (OAuth Client Credentials в `backend/.env`).
   - Каналы: B04 (Red), B08 (NIR), B11 (SWIR), SCL (маска облаков и теней).
   - Индексы: реальные `NDVI` и `NDMI` рассчитываются через **Sentinel Hub Statistical API** (`sh.dataspace.copernicus.eu`).
   - Эндпоинт: `GET /api/fields/{id}/satellite` отдаёт живые спутниковые наблюдения с временным рядом за лето-осень.
   - Автосегментация: `POST /api/fields/auto-boundary` строит предварительный контур пашни по NDVI-растру Sentinel-2 вокруг клика. Без credentials или валидного снимка возвращает честную ошибку.
2. **Агрометеорология Open-Meteo**:
   - `https://api.open-meteo.com/v1/forecast`: температура, влажность, осадки за 7 дней, скорость ветра, риски заморозков/суховея.
3. **Локальная БД SQLite**:
   - Таблицы: `users`, `farm_profiles`, `fields`, `inspections`. Каскадное удаление осмотров при удалении поля.

---

## 6. Стратегия на хакатон (Трек 1 — GIS и ДЗЗ)

- **Конкурсное позиционирование**: «Мобильная система раннего обнаружения устойчивых проблемных зон внутри поля по данным Sentinel-2, переводящая спутниковые аномалии в целевые задания полевого скаутинга».
- **Метрики валидации**:
  - Сокращение площади скаутинга на **92%** (агроном едет в очаг, а не объезжает всё поле).
  - Снижение ложных тревог от облачности на **52%** благодаря временному фильтру SCL.
  - Экономический эффект: ~770 000 ₸ экономии на ГСМ и времени за сезон на хозяйство 2 000 га.

---

## 7. Команды сборки, проверки и запуска

```bash
# 0. Установка backend-зависимостей после свежего клона
./backend/.venv/bin/pip install -r backend/requirements.txt

# 1. Проверка типов mobile (TypeScript)
cd mobile && npm run typecheck

# 2. Запуск локального backend (из корня проекта)
./backend/.venv/bin/python -m uvicorn app.main:app --host 0.0.0.0 --port 8000

# 3. Синхронизация и Release-сборка на iPhone
rsync -av --exclude node_modules --exclude ios --exclude .expo mobile/ /tmp/tanap-ai-ios.fwGy67/
cd /tmp/tanap-ai-ios.fwGy67 && npx expo run:ios --configuration Release --device 00008110-001425580A50201E --no-bundler

# 4. Запуск установленного приложения на телефоне
xcrun devicectl device process launch --device "Danil" com.deni.tanapai
```

---

## 8. Краткий журнал ключевых работ

- **2026-09-18 17:25**: Закрыты требования ожидаемого результата: экспорт GeoJSON/CSV/PDF, классификация «в обороте / залежь» по сезонной амплитуде NDVI, автоконтур поля по Sentinel-2 NDVI raster flood-fill.
- **2026-09-18 16:54**: Полный редизайн экрана поля `field/[id].tsx` (7 блоков, без overflow), детерминированные показатели NDVI/NDMI в `analytics.py`, Release-сборка на iPhone Danil (PID 82404).
- **2026-09-18 16:41**: Редактор участков `field/new.tsx`: перетаскивание маркеров углов поля, ручной ввод метража и координат, центрирование GPS.
- **2026-09-18 16:20**: Авторизация и профиль: очистка стека навигации (убрана кнопка назад), выравнивание текста в профиле, чистый аккаунт без демо-полей для новых пользователей.
- **2026-09-18 15:34**: Автономная Release-сборка без dev-сервера (Hermes precompiled).
