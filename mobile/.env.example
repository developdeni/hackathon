# Zero-config reviewer mode: use the deployed Tanap AI backend.
# scripts/start-local.sh replaces this with the local backend URL when needed.
EXPO_PUBLIC_API_URL=https://lamps-sat-increases-pencil.trycloudflare.com
